ask: разобрал Makefile (up/ps/logs) и docker-compose (healthcheck db), предложил текст раздела «Как проверить, что сервис жив», README не менял.
code: тот же разбор, сразу внёс раздел в README; разрешения перед правкой не спрашивал.
Для разбора кода без правок возьму ask.
code — правит файлы и запускает команды; ask — только читает и отвечает; plan — планирует без правок; debug — разбирает сбои.
